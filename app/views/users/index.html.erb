<p id="notice"><%= notice %></p>

<h1>High Scores</h1>

<table>
  <thead>
    <tr>
      <th>user Name</th>
      <th> user Priority</th>
      <th colspan="3"></th>
    </tr>
  </thead>
     <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.uname %></td>
        <td><%= user.phone_no %></td>
        <td><%= user.adhar_no %></td>
        
      </tr>
    <% end %>
  </tbody>
 
</table>

<br>

<%= link_to 'Create New Question', new_question_path %>
<%= link_to 'Create New Topic', new_topic_path %>
<%= link_to 'Create New Answer', new_answer_path %>

